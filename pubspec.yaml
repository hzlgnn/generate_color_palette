name: generate_color_palette
description: A simple package to generate color palettes based on a primary color.
version: 0.0.1
homepage: https://github.com/hzlgnn/generate_color_palette

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
